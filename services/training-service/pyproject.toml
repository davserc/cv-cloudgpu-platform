[project]
name = "training-service"
version = "0.1.0"
description = "Training service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn[standard]", "kafka-python", "vast-gpu-automation"]

[tool.uvicorn]
app = "app.main:app"\n