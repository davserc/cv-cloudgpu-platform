[project]
name = "evaluation-service"
version = "0.1.0"
description = "Evaluation service"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn[standard]", "kafka-python"]

[tool.uvicorn]
app = "app.main:app"
