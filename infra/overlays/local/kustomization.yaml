apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Overlay LOCAL — kind cluster con Postgres en pod.
#
# Antes del primer deploy:
#   cp infra/overlays/local/secret.yaml.tpl infra/overlays/local/secret.yaml
#   kubectl apply -f infra/overlays/local/secret.yaml
#
# Deploy:
#   kubectl apply -k infra/overlays/local/

resources:
  # Base compartida (kafka, servicios de app, observabilidad)
  - ../../k8s

  # Postgres en pod — solo en local (en cloud se usa Cloud SQL via Auth Proxy)
  - ../../k8s/services/postgres/pvc.yaml
  - ../../k8s/services/postgres/deployment.yaml
  - ../../k8s/services/postgres/service.yaml
