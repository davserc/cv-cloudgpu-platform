apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: cv-platform
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
