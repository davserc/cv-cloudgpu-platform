apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: cv-platform
spec:
  type: LoadBalancer
  selector:
    app: api-gateway
  ports:
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
