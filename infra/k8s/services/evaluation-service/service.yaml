apiVersion: v1
kind: Service
metadata:
  name: evaluation-service
  namespace: cv-platform
spec:
  type: ClusterIP
  selector:
    app: evaluation-service
  ports:
    - port: 8000
      targetPort: 8000
