apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: cv-platform
spec:
  type: ClusterIP
  selector:
    app: training-service
  ports:
    - port: 8000
      targetPort: 8000
